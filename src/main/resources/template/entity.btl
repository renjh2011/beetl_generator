${package};
<%
for(importClass in listImportClz){ %>
import  ${importClass};
<%}%>
public class ${entityName} {
    <%for(entry in attrMap){
         var key = entry.key;
         var value = entry.value;
    %>
    private  ${value}  ${key};
    <%}%>

    <%for(setMethod in listSetMethod){
    %>
    ${setMethod}
    <%}%>

    <%for(getMethod in listGetMethod){
    %>
    ${getMethod}
    <%}%>

}